<%= form_for @review do |f| %>

<%= f.label :user %>:<br />
<%= f.collection_select :user_id, User.all, :id, :name, { prompt: "Select User" }  %>

<br /><br />


<%= f.hidden_field :club_id, value: params[:club_id]%>


<br /><br />

<%= f.label :content %>:<br />
<%= f.text_area :content %>

<br /><br />

<%= f.label :rank %>:<br />
<%= f.select :rank, options_for_select(0..10), { prompt: "Select Rank" } %>


<br /><br />


<%= f.submit %>
<% end %>
